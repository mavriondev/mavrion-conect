Você está trabalhando no projeto Mavrion Conect.

TAREFA: Melhorar o algoritmo de matching em `server/routes/matching.ts`, especificamente o handler do `POST /api/matching/run` (função de loop a partir da linha ~55). Não requer nenhuma dependência nova.

**Problemas atuais a corrigir:**

1. Score fixo de 40+40+20 sem nuances
2. Match rejeitado pode reaparecer se o usuário rodar o matching novamente (linha `if (alreadyMatched) continue` ignora rejeitados, mas um novo ativo pode re-criar o match)
3. Região comparada por substring simples — "MG" não bate com "Minas Gerais"
4. Não há penalização por incompatibilidade
5. Não considera: tipo de documento do ativo, liquidez, prazo estimado do investidor

**Novo algoritmo:**
```typescript
app.post(api.matching.suggestions.run.path, async (req, res) => {
  try {
    const allAssets = await storage.getAssets();
    const allInvestors = await storage.getInvestors();
    let matchesFound = 0;

    // Mapa de normalização de estados BR
    const stateNormalization: Record<string, string[]> = {
      "MG": ["minas gerais", "mg"],
      "SP": ["são paulo", "sao paulo", "sp"],
      "RJ": ["rio de janeiro", "rj"],
      "GO": ["goiás", "goias", "go"],
      "MT": ["mato grosso", "mt"],
      "MS": ["mato grosso do sul", "ms"],
      "PR": ["paraná", "parana", "pr"],
      "RS": ["rio grande do sul", "rs"],
      "SC": ["santa catarina", "sc"],
      "BA": ["bahia", "ba"],
      "PA": ["pará", "para", "pa"],
      // adicionar demais estados conforme necessário
    };

    function matchesRegion(assetLocation: string | null, investorRegions: string[]): boolean {
      if (!assetLocation || investorRegions.length === 0) return true;
      const loc = assetLocation.toLowerCase();
      return investorRegions.some(r => {
        const rLower = r.toLowerCase();
        if (loc.includes(rLower)) return true;
        // verificar variações de nome de estado
        const variants = stateNormalization[r.toUpperCase()] || [rLower];
        return variants.some(v => loc.includes(v));
      });
    }

    for (const asset of allAssets) {
      for (const investor of allInvestors) {
        // Verificar se já existe sugestão (qualquer status) para esse par
        const existing = await db.select().from(matchSuggestions)
          .where(
            and(
              eq(matchSuggestions.assetId, asset.id),
              eq(matchSuggestions.investorProfileId, investor.id)
            )
          );
        
        // Pular se já existe (inclusive rejeitados — não re-sugerir)
        if (existing.length > 0) continue;

        let score = 0;
        const reasons: string[] = [];
        const penalties: string[] = [];

        // === CRITÉRIO 1: Tipo de ativo (0-35 pontos) ===
        const investorTypes = (investor.assetTypes as string[]) || [];
        if (investorTypes.length === 0) {
          score += 20;
          reasons.push("Investidor aceita todos os tipos de ativo");
        } else if (investorTypes.includes(asset.type)) {
          score += 35;
          reasons.push(`Tipo "${asset.type}" está nas preferências do investidor`);
        } else {
          penalties.push(`Tipo "${asset.type}" não está nas preferências do investidor`);
          // Penalização: não soma nada e score já tende a ficar abaixo do limiar
        }

        // === CRITÉRIO 2: Preço vs Ticket (0-35 pontos) ===
        if (asset.priceAsking) {
          const min = investor.ticketMin;
          const max = investor.ticketMax;
          
          if (!min && !max) {
            score += 20;
            reasons.push("Investidor não tem restrição de ticket");
          } else {
            const abaixoDoMin = min && asset.priceAsking < min * 0.8; // 20% de tolerância
            const acimaDOMax = max && asset.priceAsking > max * 1.2;
            
            if (abaixoDoMin) {
              penalties.push(`Preço R$${(asset.priceAsking/1e6).toFixed(1)}M abaixo do ticket mínimo R$${(min!/1e6).toFixed(1)}M`);
            } else if (acimaDOMax) {
              penalties.push(`Preço R$${(asset.priceAsking/1e6).toFixed(1)}M acima do ticket máximo R$${(max!/1e6).toFixed(1)}M`);
            } else {
              // Dentro do range — bonus por centralidade
              if (min && max) {
                const center = (min + max) / 2;
                const distance = Math.abs(asset.priceAsking - center) / (max - min);
                const proximity = Math.round((1 - distance) * 35); // 0-35 pontos
                score += Math.max(proximity, 15);
                reasons.push(`Preço dentro do ticket (R$${(min/1e6).toFixed(1)}M - R$${(max/1e6).toFixed(1)}M)`);
              } else {
                score += 25;
                reasons.push("Preço compatível com ticket");
              }
            }
          }
        } else {
          score += 10; // Preço não informado — pontuação neutra
          reasons.push("Preço a negociar");
        }

        // === CRITÉRIO 3: Região (0-20 pontos) ===
        const regions = (investor.regionsOfInterest as string[]) || [];
        if (regions.length === 0) {
          score += 15;
          reasons.push("Investidor opera em qualquer região");
        } else if (matchesRegion(asset.location || asset.estado, regions)) {
          score += 20;
          reasons.push(`Localização "${asset.estado || asset.location}" está no interesse do investidor`);
        } else {
          score += 0;
          penalties.push(`Região "${asset.estado || asset.location}" fora do interesse do investidor`);
        }

        // === CRITÉRIO 4: Bonus de documentação (0-10 pontos) ===
        if (asset.docsStatus === "completo" || asset.docsStatus === "regularizado") {
          score += 10;
          reasons.push("Documentação completa — menor risco");
        } else if (asset.docsStatus === "pendente") {
          score -= 5;
          penalties.push("Documentação pendente");
        }

        // === LIMIAR MÍNIMO ===
        // Só criar sugestão se score >= 40 (antes era 40 também, mas agora é mais significativo)
        if (score >= 40 && penalties.length <= 1) { // Aceita até 1 penalização
          await db.insert(matchSuggestions).values({
            orgId: asset.orgId,
            assetId: asset.id,
            investorProfileId: investor.id,
            score: Math.min(score, 100), // cap em 100
            reasonsJson: { reasons, penalties },
            status: "new",
          });
          matchesFound++;
        }
      }
    }

    res.json({ success: true, matchesFound });
  } catch (err) {
    console.error("Matching engine error:", err);
    res.status(500).json({ message: "Matching failed" });
  }
});
```

**Adicionar importação do `and` do drizzle-orm** no topo do arquivo (já deve ter `eq`):
```typescript
import { eq, and } from "drizzle-orm";
```

**Atualizar o frontend `client/src/pages/matching.tsx`:**

- Nos cards de sugestão, exibir as `penalties` além das `reasons` (as razões negativas), em vermelho, para dar contexto ao usuário sobre por que o score não é 100%
- Adicionar badge de categoria de score: "Excelente" (≥85), "Bom" (65-84), "Regular" (40-64)
- No card de um match rejeitado, não permitir "Rejeitar" novamente (já está rejeitado) — exibir botão "Arquivado" desabilitado