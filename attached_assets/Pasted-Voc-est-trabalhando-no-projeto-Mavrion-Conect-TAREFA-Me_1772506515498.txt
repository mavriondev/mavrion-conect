Você está trabalhando no projeto Mavrion Conect.

TAREFA: Melhorar o dashboard com KPIs mais ricos. A função atual `getDashboardStats()` em `server/storage.ts` (linha ~387) faz apenas 5 SELECTs com COUNT simples. O endpoint é `GET /api/stats/dashboard`.

**1. Substituir `getDashboardStats()` em `server/storage.ts` por uma versão mais rica:**
```typescript
async getDashboardStats(orgId: number) {
  const now = new Date();
  const thirtyDaysAgo = new Date(now.getTime() - 30 * 24 * 60 * 60 * 1000);
  const sixtyDaysAgo = new Date(now.getTime() - 60 * 24 * 60 * 60 * 1000);

  // KPIs básicos com período
  const [leadsTotal] = await db.select({ c: count() }).from(leads).where(eq(leads.orgId, orgId));
  const [leadsUltimos30] = await db.select({ c: count() }).from(leads)
    .where(and(eq(leads.orgId, orgId), gte(leads.createdAt, thirtyDaysAgo)));
  const [leadsAnterios30] = await db.select({ c: count() }).from(leads)
    .where(and(eq(leads.orgId, orgId), gte(leads.createdAt, sixtyDaysAgo), lt(leads.createdAt, thirtyDaysAgo)));

  // Deals ativos e valor total do pipeline
  const allDeals = await db.select().from(deals).where(eq(deals.orgId, orgId));
  const activeDeals = allDeals.filter(d => d.pipelineType === "INVESTOR");
  const pipelineValue = activeDeals.reduce((sum, d) => sum + (d.amountEstimate || 0), 0);
  const avgTicket = activeDeals.length > 0 ? pipelineValue / activeDeals.length : 0;

  // Valor ponderado por probabilidade (forecast)
  const forecastValue = activeDeals.reduce((sum, d) => {
    const prob = (d.probability || 50) / 100;
    return sum + (d.amountEstimate || 0) * prob;
  }, 0);

  // Taxa de conversão Lead → Deal (últimos 30 dias)
  const leadsQualified = await db.select({ c: count() }).from(leads)
    .where(and(eq(leads.orgId, orgId), eq(leads.status, "qualified"), gte(leads.createdAt, thirtyDaysAgo)));
  const dealsUltimos30 = await db.select({ c: count() }).from(deals)
    .where(and(eq(deals.orgId, orgId), gte(deals.createdAt, thirtyDaysAgo)));
  
  const conversionRate = Number(leadsQualified[0]?.c || 0) > 0
    ? (Number(dealsUltimos30[0]?.c || 0) / Number(leadsQualified[0]?.c || 0)) * 100
    : 0;

  // Deals por prioridade
  const dealsPorPrioridade = {
    urgent: allDeals.filter(d => d.priority === "urgent").length,
    high: allDeals.filter(d => d.priority === "high").length,
    medium: allDeals.filter(d => d.priority === "medium").length,
    low: allDeals.filter(d => d.priority === "low").length,
  };

  // Deals com due date vencido ou próximo (nos próximos 7 dias)
  const sevenDaysFromNow = new Date(now.getTime() + 7 * 24 * 60 * 60 * 1000);
  const dealsVencendo = allDeals.filter(d =>
    d.dueDate && new Date(d.dueDate) <= sevenDaysFromNow && new Date(d.dueDate) >= now
  ).length;
  const dealsVencidos = allDeals.filter(d =>
    d.dueDate && new Date(d.dueDate) < now
  ).length;

  // Crescimento mês a mês (comparativo leads)
  const crescimentoLeads = Number(leadsAnterios30[0]?.c || 0) > 0
    ? ((Number(leadsUltimos30[0]?.c || 0) - Number(leadsAnterios30[0]?.c || 0)) / Number(leadsAnterios30[0]?.c || 0)) * 100
    : 0;

  return {
    // Básicos
    leadsCount: Number(leadsTotal[0]?.c || 0),
    leadsUltimos30: Number(leadsUltimos30[0]?.c || 0),
    crescimentoLeads: Math.round(crescimentoLeads * 10) / 10,  // ex: +12.5%
    
    // Pipeline
    activeDealsCount: activeDeals.length,
    pipelineValue,
    avgTicket,
    forecastValue,
    conversionRate: Math.round(conversionRate * 10) / 10,
    
    // Alertas
    dealsVencendo,
    dealsVencidos,
    dealsPorPrioridade,
    
    // Portfolio
    assetsCount: (await db.select({ c: count() }).from(assets).where(eq(assets.orgId, orgId)))[0]?.c || 0,
    investorsCount: (await db.select({ c: count() }).from(investorProfiles).where(eq(investorProfiles.orgId, orgId)))[0]?.c || 0,
    matchesCount: (await db.select({ c: count() }).from(matchSuggestions).where(eq(matchSuggestions.orgId, orgId)))[0]?.c || 0,
  };
}
```

Importar `gte`, `lt`, `and` do drizzle-orm caso não estejam importados.

**2. Atualizar `GET /api/stats/dashboard` para passar `orgId`:**

No handler em `server/routes.ts` (ou onde estiver o endpoint), passar o orgId do usuário autenticado:
```typescript
app.get("/api/stats/dashboard", async (req, res) => {
  if (!req.isAuthenticated()) return res.status(401).json({ message: "Unauthorized" });
  const user = req.user as any;
  const stats = await storage.getDashboardStats(user.orgId);
  res.json(stats);
});
```

**3. Atualizar o frontend `client/src/pages/dashboard.tsx`:**

Adicionar novos cards e indicadores usando o retorno expandido da API:

- **Card "Valor do Pipeline"**: mostrar `pipelineValue` formatado como R$ com variação comparativa ao mês anterior
- **Card "Forecast (Ponderado)"**: mostrar `forecastValue` com tooltip explicando "Valor × Probabilidade de fechamento"
- **Card "Conversão Lead → Deal"**: mostrar `conversionRate`% com seta up/down vs mês anterior
- **Card "Ticket Médio"**: mostrar `avgTicket` formatado
- **Alerta visual**: se `dealsVencidos > 0`, mostrar banner vermelho/amarelo: "⚠️ X deal(s) com prazo vencido" com link para o CRM filtrado por vencidos
- **Alerta visual**: se `dealsVencendo > 0`, mostrar banner amarelo: "🕐 X deal(s) vencendo em 7 dias"
- **Mini gráfico de barras** (usando recharts, já disponível): distribuição de deals por prioridade usando `dealsPorPrioridade`

Manter os gráficos de área e barras de 7 dias que já existem. Adicionar os novos cards antes deles, em grid de 4 colunas no desktop.

Usar o componente `StatCard` que já existe em `client/src/components/stat-card.tsx` para os novos KPIs, adicionando uma prop `trend` (número positivo/negativo com cor verde/vermelha).