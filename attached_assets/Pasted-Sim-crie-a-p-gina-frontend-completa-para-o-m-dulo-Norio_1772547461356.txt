Sim, crie a página frontend completa para o módulo Norion Capital. Faça tudo automaticamente sem que eu precise mexer em nada.

A página deve ter 3 seções em abas:

**Aba 1 — Dashboard**
Cards com: total de operações, volume aprovado (em R$), comissão gerada (em R$), comissão a receber (em R$) e taxa de aprovação (%). Abaixo dos cards, mostrar quantas operações estão em cada etapa do kanban (identificado, diagnóstico, enviado aos fundos, em análise, aprovado, comissão gerada). Buscar os dados em GET /api/norion/dashboard.

**Aba 2 — Operações (Kanban)**
Kanban com 6 colunas, uma para cada etapa: Identificado, Diagnóstico, Enviado aos Fundos, Em Análise, Aprovado, Comissão Gerada. Cada card mostra o nome da empresa, valor solicitado e data de criação. Buscar dados em GET /api/norion/operations. Ao clicar em um card, abrir um painel lateral (drawer) com todos os detalhes da operação e botões para: mudar de etapa, editar diagnóstico e marcar comissão como recebida. As ações usam PATCH /api/norion/operations/:id.

Botão "Nova Operação" que abre um modal com formulário contendo os campos:
- Empresa (buscar pelo nome, usando GET /api/companies)
- Valor solicitado (número em R$)
- Finalidade (select: Capital de Giro, Expansão, Equipamentos, Imóvel, Agro, Outro)
- Prazo desejado (select: Até 12 meses, 12 a 24 meses, 24 a 36 meses, Acima de 36 meses)
- Garantias disponíveis (checkboxes: Imóvel, Recebíveis, Veículos, Equipamentos, Terra, Sem garantia)
- Faturamento anual (select: Até R$500k, R$500k a R$2M, R$2M a R$10M, Acima de R$10M)
- Possui dívida bancária? (sim/não)
- Observações (textarea)

Ao salvar, chamar POST /api/norion/operations.

**Aba 3 — Perfis das Empresas**
Tabela com todas as empresas listadas mostrando nome, CNPJ, setor, porte e badge colorido com o perfil Norion (verde = alto, amarelo = médio, cinza = baixo). Buscar em GET /api/companies. Botão "Recalcular Perfis" que chama POST /api/norion/recalculate-profiles e atualiza a tabela.

A página deve usar o mesmo padrão visual e componentes que já existem no projeto. Adicionar a página no menu de navegação com o nome "Norion Capital" e um ícone de cifrão ou similar. Confirme quando estiver tudo funcionando.