## Packages
recharts | Dashboard analytics and charts
framer-motion | Smooth transitions and layout animations
date-fns | Date formatting

## Notes
- Authentication uses /api/login and /api/user (session based)
- Dashboard requires aggregation of stats from /api/stats/dashboard
- Kanban board implemented with simple drag and drop state (framer-motion Reorder or simple state)
- Connectors config uses JSON editor (textarea with validation)
